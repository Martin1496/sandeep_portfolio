import Link from "next/link";
import {ArrowUpRight} from "lucide-react";
import ProjectCard from "@/components/portfolio/ProjectCard";
import {projects} from "@/lib/portfolio";
export const metadata={title:"Projects",description:"Case studies and product explorations by Sandeep Dusadh: ClipVexa, UniversalFetch, pharmacy billing software, TrustLens, Dog Walk, and Before You Buy. Each project states its current stage.",alternates:{canonical:"/projects"}};
export default function Projects(){return <><section className="page-hero container"><span className="eyebrow">PORTFOLIO / SELECTED WORK</span><h1>Built today.<br/><em>Imagined next.</em></h1><p>Each case study explains the problem, approach, technology, and current stage. In-progress builds and concepts are labeled so you can judge the work fairly.</p></section><section className="section container"><div className="section-top"><div><span className="eyebrow">ACTIVE BUILDS & PROTOTYPES</span><h2>Four featured projects.</h2></div><p className="section-intro">Explore the working web previews of ClipVexa, UniversalFetch and TrustLens, plus an earlier PharmacyDesk desktop demo. Each page explains what still needs to be finished.</p></div><div className="project-grid">{projects.slice(0,4).map((p,i)=><ProjectCard key={p.slug} project={p} index={i}/>)}</div></section><section className="section statement-section"><div className="container"><div className="section-top"><div><span className="eyebrow">PRODUCT EXPLORATIONS</span><h2>Future concepts.</h2></div><p className="section-intro">Dog Walk and Before You Buy are early ideas, not launched client products.</p></div><div className="project-grid concept-grid">{projects.slice(4).map((p,i)=><ProjectCard key={p.slug} project={p} index={i+4} compact/>)}</div></div></section><section className="cta-panel container"><span className="eyebrow">YOUR PROJECT COULD BE NEXT</span><h2>Tell me what you need built.</h2><p>Share the workflow, user, and outcome you care about. I can help define the most useful first version.</p><Link className="button" href="/contact">Discuss a project <ArrowUpRight size={18}/></Link></section></>}
